spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bankdb?serverTimezone=Asia/Seoul
    username: jihwan
    password: hwani

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
